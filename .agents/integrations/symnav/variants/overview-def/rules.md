# overview-def symnav integration

Always read `.agents/integrations/symnav/variants/overview-def/skill/SKILL.md` before starting work. `symnav overview` and `symnav def` commands are installed globally.

Use normal reads, search, tests, and edits whenever they help.
