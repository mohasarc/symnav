export function configure(
  host: string,
  port: number,
): void {
  void host;
  void port;
}

export class Server {
  start(
    host: string,
    port: number,
  ): void {
    void host;
    void port;
  }
}
